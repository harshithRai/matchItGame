enum CustomDialogType { levelCompleted, levelFailed, achievement, leaderboard }

enum ConnectivityStatus { WiFi, Cellular, Offline }

enum DeviceScreenType { Mobile, Tablet, Desktop }

enum CardType { Text, FitText, Image }
